---
import MainGridLayout from "../layouts/MainGridLayout.astro";
import { url } from "../utils/url-utils";

const pageTitle = "OpenCLI：项目介绍与使用指南";
const pageDescription =
	"基于 jackwener/opencli 的功能定位、适用场景、安装方式与常见用法整理的一页中文介绍，已直接集成到博客中。";
const searchKeywords = [
	"OpenCLI",
	"AI CLI",
	"AI Agent",
	"Claude Code",
	"Cursor",
	"浏览器自动化",
	"Browser Bridge",
	"Electron",
	"Electron CLI",
	"Chrome 登录态复用",
	"网站转 CLI",
	"命令行工具",
	"自动化工具",
	"CLI Hub",
];
const aboutUrl = url("/about/");
---

<MainGridLayout title={pageTitle} description={pageDescription}>
	<script>
		import("../scripts/right-sidebar-layout.js");
	</script>

	<div class="sr-only" aria-hidden="true" data-pagefind-meta="keywords">
		{searchKeywords.join(" · ")}
	</div>

	<div class="flex w-full rounded-[var(--radius-large)] overflow-hidden relative min-h-32" data-pagefind-body>
		<div class="card-base z-10 px-6 py-6 md:px-9 md:py-8 relative w-full space-y-8">
			<section class="space-y-4">
				<div class="sr-only">
					关键词：OpenCLI、AI CLI、AI Agent、Claude Code、Cursor、浏览器自动化、Browser Bridge、Electron CLI、Chrome 登录态复用、网站转 CLI、命令行工具、自动化工具、CLI Hub。
				</div>
				<div class="inline-flex items-center gap-2 rounded-full border border-[var(--line-divider)] bg-[var(--card-bg)] px-3 py-1 text-sm text-75">
					<span class="h-2 w-2 rounded-full bg-[var(--primary)]"></span>
					GitHub 项目解读
				</div>
				<h1 class="text-3xl font-bold text-90 md:text-5xl leading-tight">
					OpenCLI 是什么，适合谁用，怎么快速上手
				</h1>
				<p class="max-w-3xl text-base leading-8 text-75 md:text-lg">
					<a
						href="https://github.com/jackwener/opencli"
						target="_blank"
						rel="noreferrer"
						class="font-medium text-[var(--primary)] underline decoration-[color:var(--primary)]/30 underline-offset-4"
					>
						jackwener/opencli
					</a>
					是一个偏工程化的命令行工具项目，核心方向不是做一个简单的 shell 包装器，而是把 AI、CLI 工作流和自动化场景整合到一个更可编排的入口里。对已经习惯终端、脚本和开发工具链的人来说，它更像一把可扩展的“工作台”，而不是单一命令。
				</p>
			</section>

			<section class="grid gap-4 md:grid-cols-3">
				<div class="rounded-[24px] border border-[var(--line-divider)] bg-[var(--card-bg)] p-5">
					<div class="text-sm text-50">项目定位</div>
					<div class="mt-2 text-lg font-semibold text-90">AI + CLI + 自动化入口</div>
				</div>
				<div class="rounded-[24px] border border-[var(--line-divider)] bg-[var(--card-bg)] p-5">
					<div class="text-sm text-50">适合人群</div>
					<div class="mt-2 text-lg font-semibold text-90">开发者 / 运维 / 高阶终端用户</div>
				</div>
				<div class="rounded-[24px] border border-[var(--line-divider)] bg-[var(--card-bg)] p-5">
					<div class="text-sm text-50">使用方式</div>
					<div class="mt-2 text-lg font-semibold text-90">本地安装后在命令行调用</div>
				</div>
			</section>

			<section class="grid gap-6 lg:grid-cols-[1.15fr_0.85fr]">
				<div class="rounded-[28px] border border-[var(--line-divider)] bg-gradient-to-br from-[var(--card-bg)] to-[var(--btn-plain-bg)] p-6 md:p-7 space-y-4">
					<h2 class="text-2xl font-semibold text-90">一句话理解 OpenCLI</h2>
					<p class="leading-8 text-75">
						如果您平时已经在终端里做开发、脚本、部署、调试、内容生成、工具调用，那么 OpenCLI 的价值在于：把这些原本分散的命令、模型和工作流，收进一个更统一的入口里。
					</p>
					<p class="leading-8 text-75">
						它的重点不只是“能调用 AI”，而是“让 AI 更像命令行工具的一部分”，这样它就更适合进入真实的工程流程，而不是只停留在网页聊天层。
					</p>
				</div>

				<div class="rounded-[28px] border border-[var(--line-divider)] bg-[var(--card-bg)] p-6 md:p-7 space-y-4">
					<h2 class="text-2xl font-semibold text-90">适合的场景</h2>
					<ul class="space-y-3 text-75 leading-7">
						<li>• 在终端里统一做代码生成、改写、解释与执行</li>
						<li>• 把 AI 能力嵌进现有脚本、自动化或运维流程</li>
						<li>• 需要更可控、更工程化的本地工作流</li>
						<li>• 希望把“问模型”变成“调用工具”的习惯</li>
					</ul>
				</div>
			</section>

			<section class="space-y-4">
				<h2 class="text-2xl font-semibold text-90">安装思路</h2>
				<div class="rounded-[28px] border border-[var(--line-divider)] bg-[var(--card-bg)] p-6 space-y-5">
					<p class="leading-8 text-75">
						具体安装命令以项目 README 为准。最稳的做法是先打开仓库文档，确认它当前推荐的是哪一种安装方式，再按您的系统环境执行。
					</p>
					<div class="grid gap-4 md:grid-cols-2">
						<div class="rounded-[20px] bg-[var(--btn-plain-bg)] p-4">
							<div class="text-sm font-medium text-50">步骤 1</div>
							<div class="mt-2 text-90 leading-7">先看 GitHub README，确认依赖、运行方式、是否有 Node / 包管理器版本要求。</div>
						</div>
						<div class="rounded-[20px] bg-[var(--btn-plain-bg)] p-4">
							<div class="text-sm font-medium text-50">步骤 2</div>
							<div class="mt-2 text-90 leading-7">按仓库文档安装后，先跑最小命令，确认 CLI 已经进入可调用状态。</div>
						</div>
					</div>
					<pre class="overflow-x-auto rounded-[20px] bg-slate-950 px-4 py-4 text-sm leading-7 text-slate-100"><code># 先看项目说明
open https://github.com/jackwener/opencli

# 按 README 安装
# 然后先跑帮助命令确认是否可用
opencli --help</code></pre>
				</div>
			</section>

			<section class="space-y-4">
				<h2 class="text-2xl font-semibold text-90">怎么使用</h2>
				<div class="grid gap-4 md:grid-cols-2 xl:grid-cols-3">
					<div class="rounded-[24px] border border-[var(--line-divider)] bg-[var(--card-bg)] p-5 space-y-2">
						<h3 class="text-lg font-semibold text-90">1. 当统一入口</h3>
						<p class="text-75 leading-7">把原来分散的模型调用、脚本执行、命令行交互收进一个固定入口里。</p>
					</div>
					<div class="rounded-[24px] border border-[var(--line-divider)] bg-[var(--card-bg)] p-5 space-y-2">
						<h3 class="text-lg font-semibold text-90">2. 当自动化胶水层</h3>
						<p class="text-75 leading-7">让 AI 参与您已有的 shell、脚本、工程工具链，而不是单独再开一个网页窗口。</p>
					</div>
					<div class="rounded-[24px] border border-[var(--line-divider)] bg-[var(--card-bg)] p-5 space-y-2">
						<h3 class="text-lg font-semibold text-90">3. 当个人工作台</h3>
						<p class="text-75 leading-7">把高频动作沉淀成命令，后面复用，比一次次重新描述需求更稳。</p>
					</div>
				</div>
			</section>

			<section class="space-y-4">
				<h2 class="text-2xl font-semibold text-90">上手建议</h2>
				<div class="rounded-[28px] border border-[var(--line-divider)] bg-[var(--card-bg)] p-6 space-y-4">
					<ol class="space-y-3 text-75 leading-8 list-decimal pl-5">
						<li>先只解决一个真实问题，比如“生成命令”“解释错误”“整理脚本”。</li>
						<li>别一开始就做很重的自动化，先把帮助命令、基础调用、输入输出摸清楚。</li>
						<li>如果要长期用，把高频命令记录下来，逐步形成自己的 CLI 工作流。</li>
					</ol>
				</div>
			</section>

			<section class="rounded-[28px] border border-[var(--line-divider)] bg-[var(--card-bg)] p-6 md:p-7 space-y-4">
				<h2 class="text-2xl font-semibold text-90">什么时候值得试</h2>
				<p class="leading-8 text-75">
					如果您已经不满足于“打开网页问一句”的使用方式，而是希望 AI 真正进入日常开发与自动化流程，那 OpenCLI 值得试。
				</p>
				<p class="leading-8 text-75">
					如果您只是偶尔问几个问题，那它未必比现成聊天界面更轻；但如果您重视命令行、一致性和可复用流程，它的味道就出来了。
				</p>
				<div class="flex flex-wrap gap-3 pt-2">
					<a
						href="https://github.com/jackwener/opencli"
						target="_blank"
						rel="noreferrer"
						class="inline-flex items-center justify-center rounded-full bg-[var(--primary)] px-5 py-3 text-sm font-medium text-white transition hover:opacity-90"
					>
						查看 GitHub 项目
					</a>
					<a
						href={aboutUrl}
						class="inline-flex items-center justify-center rounded-full border border-[var(--line-divider)] px-5 py-3 text-sm font-medium text-75 transition hover:bg-[var(--btn-plain-bg)]"
					>
						返回博客
					</a>
				</div>
			</section>
		</div>
	</div>
</MainGridLayout>
